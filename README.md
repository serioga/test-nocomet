* Launch sbt.
* Execute `~jetty-run` command
* Open http://localhost:8080/test in the browser
* Modify any source file, i.e src/main/webapp/test.html

After these steps jetty is reloaded and page is reloaded but <code>LiftRules.noCometSessionPage = "/nocomet"</code> is ignored.

